baseline_packages:
  - git
  - curl-minimal
  - vim
  - htop
  - net-tools

baseline_users:
  - name: devops
    shell: /bin/bash
    groups: wheel
  - name: deploy
    shell: /bin/bash
    groups: wheel

motd_message: |
  This server is managed by Ansible.
  Unauthorized access is prohibited.
